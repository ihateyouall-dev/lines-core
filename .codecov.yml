codecov:
  notify:
    after_n_builds: 1

coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%

ignore:
  - "tests/**"
  - "build/**"
